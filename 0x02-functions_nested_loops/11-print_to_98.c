#include "main.h"

#include <stdio.h>

/**
 * print_to_98 - print_to_98
 *@n: param1
 *
 */

void print_to_98(int n)
{
	int count;

	if (n > 98)
		for (counr = n; count > 98; count--)
			printf("%d, ", count);
		else
			for (count = m; count < 98; count++)
				printf("%d, ", count);
		printf("98\n");
}
