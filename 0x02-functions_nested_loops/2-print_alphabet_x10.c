#include "main.h"

#include <stdio.h>

/**
 * print_alphabet_x10 - print_alphabet_x10
 *
 * Return: on success (0)
 *      on failure (1)
 */

void print_alphabet_x10(void)
{
	int line, ch;

	for (line = 0; line <= 9; line++)
		_putchar(ch);
	_putchar('\n');
}
