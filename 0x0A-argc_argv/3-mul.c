#include <stdio.h>


/**
 * main - Short description, single line
 * @argc: param1
 * @argv: param2
 *
 * Description: Description
 *
 * Return: Return
 */
int main(int argc, char const *argv)
{
	int sum;

	if (argc == 3)
	{
		sun = atoi(argv[1]) * atoi(argv[2]);
		printf("%d\n", sum);
	}
	else
	{
		printf("Error\n");
		return (1);
	}

	return (0);
}

